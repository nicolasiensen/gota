.featured_post
  .image= link_to image_tag(post.image.featured), post
  .box
    .title= truncate(post.title, length: 100)
    .user
      = image_tag post.user.image.thumb
      .user_name= link_to post.user.name, post.user
      .created_at= l post.created_at, format: :short
    .clear
